# Tarea 01, clave 208999

El modelo tiene buenos resultados locales y los artefactos se pueden reproducir.
La entrega aún requiere resolver los pendientes personales y oficiales de
[la revisión de requisitos](REVISION_REQUISITOS_208999.md).

La copia de trabajo está en `C:\Development\School\mineria_datos`.
La rama es [`tarea-01-208999`](https://github.com/HarryGomar/mineria_datos/tree/tarea-01-208999).
El cambio de nombre conserva su historial. `main` conserva su trabajo anterior.

Desde PowerShell, la primera vez:

```powershell
Set-Location -LiteralPath 'C:\Development\School\mineria_datos'
uv sync --frozen
.\abrir-tarea-01.ps1
```

`Set-Location` entra en el repositorio. `uv sync --frozen` instala las versiones
de `uv.lock` dentro de `.venv`. El último comando registra el kernel local y abre
el notebook, sin ejecutar sus celdas. Selecciona **Python 3.13 (mineria-datos)**.

Para recalcular todo desde un kernel nuevo:

```powershell
.\ejecutar-tarea-01.ps1
```

El script registra el kernel, ejecuta las 15 celdas de código, regenera tablas,
20 PNG y predicciones, y ejecuta el verificador. Para comprobar los artefactos
ya generados sin volver a entrenar:

```powershell
.\.venv\Scripts\python.exe tareas/verificar_tarea_01.py
```

La clave `208999` corresponde a `v2e419aae`. La fórmula usa mínimos cuadrados
sobre siete columnas: `x11`, `x2²`, `sin(2π*x4)`, `log(x6)`, `x7³`, `1(x9>60)`
y `x1*x11`, más intercepto.

| Modelo | MSE CV de desarrollo | MSE de auditoría reutilizada | R² de auditoría |
|---|---:|---:|---:|
| Baseline, 12 variables crudas | 104.6655 | 99.6679 | 0.3938 |
| Compacto, 7 columnas | 19.5950 | 19.3227 | 0.8825 |
| Polinomio grado 3 + LassoCV | 57.3492 | 58.0620 | 0.6469 |

La reducción de MSE de auditoría frente al baseline es **80.61%**. Lasso conserva
34 de 454 columnas con `alpha = 0.525217`. El RMSE del compacto es 4.3958.

Las 600 filas de desarrollo se usaron para seleccionar la fórmula; su CV es
descriptiva después de la selección. Las otras 200 filas ya se consultaron en la
versión anterior. Reproducir el resultado no crea una prueba nueva. La revisión
conserva la fórmula y las predicciones, salvo redondeo numérico.

Empieza por estas figuras:

- [Mejora en cada paso](resultados_208999/10_progreso_modelos.png).
- [Interacción antes y después](resultados_208999/06_interaccion.png).
- [Diagnóstico de auditoría](resultados_208999/08_auditoria.png).
- [Comparación con Lasso](resultados_208999/09_comparacion_lasso.png).
- [Contribución de cada columna](resultados_208999/11_ablacion_terminos.png).

El [archivo de entrega](../entregas/tarea-01/v2e419aae/predicciones.csv) contiene una
columna `y` y 400 filas, en el orden de `test.csv`. El ajuste que lo produce usa
las 800 filas de `train.csv`.

El [README del curso](tarea_01_README.md) establece la entrega mediante PR al
repositorio del profesor. `/validar` obtiene la meta oficial sin consumir intentos;
`/calificar` consume uno de los tres. Esta revisión no abrió un PR ni envió
comentarios al bot. El MSE meta y la calificación oficial siguen sin conocerse.

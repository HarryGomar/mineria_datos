# Tarea 01 · solución local para 208999

Abre PowerShell en `C:\Work\School\Mineria` y ejecuta:

```powershell
.\abrir-tarea-01.ps1
```

Este comando abre JupyterLab con el notebook de la tarea; no ejecuta las celdas ni envía una entrega.
Selecciona el kernel **Python 3.13 (mineria-datos)** si Jupyter lo solicita.

El notebook tiene `CLAVE_UNICA = "208999"`, correspondiente a la variante `v2e419aae`.
El modelo de entrega usa `LinearRegression` (mínimos cuadrados ordinarios),
con siete columnas: `x11`, `x2²`, `sin(2π*x4)`, `log(x6)`, `x7³`, `1(x9>60)` y `x1*x11`.
Las transformaciones están fijadas; los coeficientes se estiman por regresión lineal.
La Parte 4 incluye además el pipeline exigido por el enunciado:
`PolynomialFeatures(3) → StandardScaler → LassoCV`, con selección de penalización,
conteo de coeficientes no nulos y comparación externa contra el modelo guiado.
Los cambios están en la rama local `codex/tarea-01-208999`.

Para ejecutar todo y regenerar los resultados:

```powershell
.\ejecutar-tarea-01.ps1
```

Este comando hace la exploración, compara regresiones mediante CV de 5 partes repetida
4 veces sobre 600 filas, evalúa la especificación congelada en las otras 200 filas,
reajusta los coeficientes con las 800 filas y escribe 400 predicciones. Al terminar,
el verificador independiente comprueba los resultados. Todo ocurre localmente.

| Modelo | MSE CV en desarrollo | MSE de auditoría | R² de auditoría |
|---|---:|---:|---:|
| Baseline: 12 variables crudas | 104.6655 | 99.6679 | 0.3938 |
| Modelo compacto: 7 columnas | 19.5950 | 19.3227 | 0.8825 |
| Polinomio grado 3 + LassoCV | 57.3492 | 58.0620 | 0.6469 |

Lasso eligió `alpha = 0.525217` y conservó 34 de las 454 columnas. El modelo compacto
tuvo menor error en desarrollo y auditoría, por lo que se conserva para la entrega.

La reducción del MSE de auditoría es 80.61%. La especificación se seleccionó antes de
consultar esas 200 filas. Los MSE locales no son la calificación oficial ni garantizan
un óptimo absoluto. La selección favorece un modelo compacto; el notebook explica
los modelos más grandes y las hipótesis descartadas.

Resultados:

- `entregas/tarea-01/v2e419aae/predicciones.csv`: archivo local con columna `y`, sin índice.
- `tareas/resultados_208999/metricas.json`: métricas y protocolo de evaluación.
- `tareas/resultados_208999/coeficientes.csv`: intercepto y coeficientes del ajuste con 800 filas.
- `tareas/resultados_208999/`: comparaciones, bitácora y figuras.

El notebook está ejecutado y contiene sus resultados. El análisis y la bitácora se
identifican como asistidos por IA: revísalos y entiéndelos antes de entregar.
La Parte 4 implementa LassoCV y responde las cuatro preguntas de comparación. Los resultados
incluyen `coeficientes_lasso.csv`, `lasso_cv_externo.csv` y `auditoria.csv`.
Consulta `REVISION_REQUISITOS_208999.md` para ver la correspondencia con cada requisito
y los aspectos personales u oficiales que no pueden verificarse localmente.

Se han generado predicciones localmente, sin solicitar una calificación ni consumir intentos.

La entrega y la calificación son pasos separados. Consulta `tarea_01_README.md` antes
de hacerlos: `/validar` revisa el formato sin gastar intentos y `/calificar` sí gasta
uno de los tres intentos. No ejecutes `/calificar` hasta decidir que quieres calificar.
